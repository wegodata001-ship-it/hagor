import { PrismaClient } from "@prisma/client";

export async function seedFashionPreset(_prisma: PrismaClient, _storeId: string): Promise<void> {
  void _prisma;
  void _storeId;
  // Reserved preset slot for future business types.
}
